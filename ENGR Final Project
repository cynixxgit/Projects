
#include <iostream>
#include <cmath>
#include <iomanip>
#include <ctime>
#include <string>
using namespace std;

#include <iomanip>

int main()
{
    int times = 1;
    double velocity = (0.00001 * pow(times, 3)) - (0.00488 * pow(times, 2)) + (0.75795 * times) + 181.3566;
    double acceleration = 3 - (0.000062 * pow(velocity, 2));

    cout << velocity << " " << acceleration << endl;
    
    return 0;
}
